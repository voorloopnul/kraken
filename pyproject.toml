[project]
name = "kraken"
version = "0.1.0"
description = "Kraken desktop front-end for the Pi coding agent"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pygments>=2.20.0",
    "pyside6>=6.11.1",
]
